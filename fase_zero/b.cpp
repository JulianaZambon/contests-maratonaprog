#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main()
{
    int a, b, c;
    char d;
    scanf("%d %d %d %c", &a, &b, &c, &d);
    3 * a == 2 * b;
    2 * c == 5 * a;
    if (d == a) /*ru vou verificar se o char lido eh igual*/
    {
        /*maximiza*/
        
    }
    else if (d == b)
    {
    }
    else
    {
    }
    return 0;
}